name: Snapchat Decompiler (Final Fix)
on:
  workflow_dispatch:
    inputs:
      apk_url:
        description: 'Direct URL to the APK'
        required: true

jobs:
  decompile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install and Patch JADX
        run: |
          wget https://github.com/skylot/jadx/releases/download/v1.5.0/jadx-1.5.0.zip
          unzip jadx-1.5.0.zip -d jadx
          
          # PHYSICALLY REMOVE the Kotlin Metadata plugin to prevent the crash
          # This forces JADX to treat Kotlin classes as plain Java.
          rm jadx/lib/jadx-kotlin-metadata-*.jar
          
          echo "$GITHUB_WORKSPACE/jadx/bin" >> $GITHUB_PATH

      - name: Download APK
        run: |
          curl -L -o snapchat.apk "${{ github.event.inputs.apk_url }}"
          if [ ! -s snapchat.apk ]; then echo "Download failed"; exit 1; fi

      - name: Decompile
        run: |
          export JAVA_OPTS="-Xmx6G -XX:+UseG1GC"
          
          # We only use basic, universally accepted flags now
          jadx -d out_folder snapchat.apk \
            --no-res \
            --no-imports \
            --show-bad-code \
            --log-level error

      - name: ZIP and Upload
        run: |
          zip -r source_code.zip out_folder/
          
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: snapchat-java-source
          path: source_code.zip
